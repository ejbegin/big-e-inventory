[project]
name = "big-e-inventory"
version = "0.1.0"
description = "Big E Inventory Management Application"
dependencies = [
    "flask",
    "squareup",
    "python-dotenv",
    "authlib",
    "requests",
    "pillow>=12.3.0",
]

[tool.pyright]
venvPath = "."
venv = ".venv"
